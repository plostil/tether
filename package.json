{
  "name": "tether",
  "version": "0.0.1",
  "private": true,
  "description": "Cross-device continuity platform — phone <-> PC view/control, I/O routing, session handoff",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/server",
    "apps/reference-cli",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/server",
    "build:web": "npm run build -w apps/web",
    "test": "npm test -w apps/server && npm test -w apps/web",
    "typecheck": "npm run typecheck -w packages/protocol && npm run typecheck -w apps/server && npm run typecheck -w apps/reference-cli && npm run typecheck -w apps/web"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0"
  }
}
