// Root Gradle build. Plugin versions are pinned here; the app module applies them.
plugins {
    id("com.android.application") version "8.6.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
